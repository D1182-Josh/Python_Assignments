    #TASK: 
    # Write python code that writes in descending order from 1 to 10





       SOLUTION1:

        sıra = []
        for i in range(0, 11):
          sıra.append(i)
        sıra = sorted(sıra, reverse=True)
        sıra


        SOLUTION2:
        
        liste = sorted([i for i in range(1,11)], reverse = True)
        liste
